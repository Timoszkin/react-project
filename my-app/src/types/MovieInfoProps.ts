// use type Movie from api/types.ts instead
// DELETE LATER

export type MovieInfoProps = {
  id: number,
  name: string;
  posterPath: string,
  plot: string,
  rating: number,
  director: string,
  year: number,
}
